<%= form_with(model: [@spot, @comment], url: spot_comments_path(@spot)) do |f| %>
    <%=f.text_area :comment, class: 'form-control', placeholder: "スポットに対してコメントできるよ！" %>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>
