<%= form_with(model: [@spot, @comment]) do |f| %>
    <%=f.text_area :comment, class: 'form-control fs-4', placeholder: "スポットに対してコメントできるよ！" %>
    <%= f.submit "コメント", class: "rounded-pill btn btn-warning text-white btn-lg float-right" %>
<% end %>
